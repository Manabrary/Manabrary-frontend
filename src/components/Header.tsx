import * as styles from './Header.css.ts';

export default function Header(){
    return(
        <header className={styles.header}>
            <h1 className={styles.headerTitle}>Manabrary</h1>
        </header>
    )
}